# Security Configuration
# Comma-separated list of allowed domains
ALLOWED_DOMAINS=github.com,gitlab.com,bitbucket.org

# Max file size in KB (default: 1024 = 1MB)
# Max file size in KB (default: 1024 = 1MB)
# MAX_FILE_SIZE=1024 (Deprecated)

# Caching Configuration
# Time to live in seconds (default: 86400 = 24h). Set to 0 to disable.
# Time to live in seconds (default: 86400 = 24h). Set to 0 to disable.
# CACHE_TTL_SECONDS=86400 (Deprecated, use CACHE_TTL_MIN_SECONDS and CACHE_TTL_PER_BYTE)
# Redis URL (optional). If not set, uses in-memory cache.
# REDIS_URL=redis://localhost:6379

# Minimum cache TTL in seconds (default: 3600 = 1h)
CACHE_TTL_MIN_SECONDS=3600
# Cache TTL scaling factor (seconds per byte)
CACHE_TTL_PER_BYTE=0.0001
# Analysis timeout in seconds (default: 300 = 5m)
ANALYSIS_TIMEOUT_SECONDS=300
# Cache TTL for timed out repositories in seconds (default: 300 = 5m)
CACHE_TTL_ON_TIMEOUT=300

# Base URL for SEO and Metadata
NEXT_PUBLIC_BASE_URL=https://example.com
